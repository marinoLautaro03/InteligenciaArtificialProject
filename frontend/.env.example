# No public environment variables are required right now.
# If the frontend starts consuming the backend from a different origin,
# add VITE_API_URL with the backend base URL.
# VITE_API_URL=http://localhost:3001
